[project]
name = "warden-harness"
version = "0.1.0"
description = "Test harness for Warden MCP server"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
warden-harness = "harness.run:main"
