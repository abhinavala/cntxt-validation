import type { WardenSharedPackage } from '@warden/shared';

const pkg: WardenSharedPackage = {
  name: '@warden/server',
  version: '0.1.0',
};

console.log(`${pkg.name}@${pkg.version} starting...`);

export {};
