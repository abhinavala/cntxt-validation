// Stub — real implementation provided by the credential-index task (F2).
// This file exists so the module can be resolved and mocked in tests.
export function getRegisteredValues(): string[] {
  return [];
}
